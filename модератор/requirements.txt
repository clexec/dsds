aiogram==3.20.0
aiohttp==3.11.18

# ─── Redis / кэш / лимиты ────────────────────────────────────────────────────
redis>=5.0.0              # redis.asyncio для async Redis
aiolimiter>=1.1.0         # локальные async rate-limits (один инстанс)
aioredlock>=0.7.3         # Redlock — распределённые блокировки через Redis

# ─── Файловые блокировки ─────────────────────────────────────────────────────
portalocker>=2.8.2        # кросс-платформенные файловые блокировки (fcntl + win32)

# ─── Интернационализация ─────────────────────────────────────────────────────
python-i18n[YAML]>=0.3.9  # i18n с поддержкой YAML-локалей
PyYAML>=6.0               # парсинг .yml файлов локалей
